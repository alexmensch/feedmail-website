{
  "name": "feedmail-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "deploy": "pnpm run build && npx wrangler deploy",
    "clean": "rm -rf _site"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "sass": "^1.97.3"
  },
  "devDependencies": {
    "wrangler": "^4.14.4"
  },
  "packageManager": "pnpm@10.30.2"
}
